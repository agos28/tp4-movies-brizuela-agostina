import { stackSansText } from '@/lib/fonts';

const Footer = () => {
  return (
    <footer className={`${stackSansText.className} h-12 bg-[#90a955] flex justify-center items-center text-white`}>
        <p>Enjoy !!</p>
    </footer>
  );
};

export default Footer;
