import Link from 'next/link';

import { bowlbyOne } from '@/lib/fonts'

const Navbar = () => {
  

  return (
    <header className={`${bowlbyOne.className} `}>
      <nav>
        <ul className=''>
          
        </ul>
      </nav>
    </header>
  );
};

export default Navbar;
